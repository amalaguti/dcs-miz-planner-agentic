## 1. Extract + cache

- [x] 1.1 Add `pypdf` + `DocTextCache` (path/mtime/size) + extract helper with caps
- [x] 1.2 Wire `list_installed_campaigns(include_doc_text=…)` + tool bridge param
- [x] 1.3 Update prompts/tool description for opt-in cached excerpts

## 2. Tests & finish

- [x] 2.1 Hermetic PDF fixture tests (default off, extract on, cache hit)
- [x] 2.2 Docs; pytest; archive; merge (no push)
