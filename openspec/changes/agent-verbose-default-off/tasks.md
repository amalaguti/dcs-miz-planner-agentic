## 1. Implementation

- [x] 1.1 Set `DEFAULT_VERBOSE = False`; update module docstring
- [x] 1.2 CLI `plan` / `chat` `--verbose` default + help text (default off; `--verbose` to enable)
- [x] 1.3 Chat help / banner copy: default off
- [x] 1.4 Tests: default off + slash toggle still works; assert DEFAULT_VERBOSE is False
- [x] 1.5 README + BACKLOG `#10b` → done

## 2. Verification

- [x] 2.1 `uv run pytest -q` (chat / agent related at minimum; full suite before finish)
